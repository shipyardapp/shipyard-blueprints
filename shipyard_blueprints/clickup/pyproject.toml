[project]
name = "shipyard-clickup"
version = "0.1.1"
description = ""
requires-python = ">=3.9"
dependencies = [
    "datetime>=5.5",
    "requests>=2.32.3",
    "shipyard-templates>=0.1.5",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "python-dotenv>=1.0.1",
]
