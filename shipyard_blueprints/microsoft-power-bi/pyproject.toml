[project]
name = "shipyard-microsoft-power-bi"
version = "0.2.0a0"
description = ""
requires-python = ">=3.9"
dependencies = [
    "msal>=1.26.0",
    "shipyard-templates>=0.9.0",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.0.0",
    "pytest>=8.3.2",
]
