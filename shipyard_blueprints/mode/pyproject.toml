[tool.poetry]
name = "shipyard-mode"
version = "0.1.1a0"
description = "A local client for connecting and working with Mode"
authors = ["wrp801 <wespoulsen@gmail.com>"]
license = "Apache 2.0"
packages = [{ include = "shipyard_mode" }]

[tool.poetry.dependencies]
python = ">=3.9,<3.10"
shipyard-bp-utils = "^1.2.0"
shipyard-templates = "^0.7.0"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
